services:
  cortex:
    build: .
    ports:
      - "127.0.0.1:7100:7100"
    volumes:
      - cortex-data:/app/data
    env_file: .env
    environment:
      CORTEX_BIND_HOST: "0.0.0.0"
      CORTEX_ALLOW_NONLOOPBACK: "1"
    restart: unless-stopped

volumes:
  cortex-data:
